# Copie este arquivo para .env e preencha sua chave da OpenAI.
# Só é necessária para o fallback Whisper (vídeos SEM legenda).
# Vídeos com legenda funcionam sem nenhuma chave.

OPENAI_API_KEY=sk-...

# Opcional: modelo de transcrição (padrão: whisper-1)
# OPENAI_TRANSCRIBE_MODEL=whisper-1
